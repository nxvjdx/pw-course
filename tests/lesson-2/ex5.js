// Lặp từ 1 tới 100
for (let i = 1; i <= 100; i++) {
    // Kiểm tra số chẵn hay lẻ
    if (i % 2 === 0) {
      console.log(`Số ${i} là số chẵn`);
    } else {
      console.log(`Số ${i} là số lẻ`);
    }
  }
  